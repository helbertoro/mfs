import React from 'react';
import PropTypes from 'prop-types';
import './Banner.sass';

export const Banner = (props) => {
  return (
    <p>Banner</p>
  );
};

Banner.propTypes = {

};

Banner.defaultProps = {

};