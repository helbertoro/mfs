import React from 'react';
import PropTypes from 'prop-types';
import './Footer.sass';

export const Footer = (props) => {
  return (
    <p>Footer</p>
  );
};

Footer.propTypes = {

};

Footer.defaultProps = {

};
